---
plugin: servicenow.itsm.now
fields: [ip_address,fqdn,host_name,sys_class_name,name,os,install_status,attributes,category]
query:
  - category: = lights
keyed_groups:
  - key: category
    prefix: category